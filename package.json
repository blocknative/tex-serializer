{
  "name": "serializer",
  "version": "0.0.2",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "prepare": "npm run build"
  },
  "types": "./types.d.ts",
  "files": [
    "dist",
    "types.d.ts"
  ]
}
